const bundleDir = './dist-tmp/';
const outputDir = './dist/';
const defaultArtifactType = '-SNAPSHOT';

module.exports = {
  bundleDir: bundleDir,
  outputDir: outputDir,
  defaultArtifactType: defaultArtifactType
};